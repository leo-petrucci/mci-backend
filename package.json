{
  "name": "prisma-backend",
  "license": "MIT",
  "scripts": {
    "start": "node dist/server",
    "clean": "rm -rf dist",
    "build": "npm -s run clean && npm -s run generate && tsc",
    "generate": "npm -s run generate:prisma && npm -s run generate:nexus",
    "generate:prisma": "prisma generate",
    "generate:nexus": "ts-node --transpile-only src/schema",
    "test": "jest",
    "dev": "ts-node-dev --no-notify --respawn --transpileOnly src/server",
    "netlify:build": "netlify-lambda build",
    "netlify:serve": "netlify-lambda serve"
  },
  "dependencies": {
    "@netlify/zip-it-and-ship-it": "^1.3.12",
    "@nexus/schema": "0.15.0",
    "aws-lambda": "^1.0.6",
    "axios": "^0.19.2",
    "bcryptjs": "2.4.3",
    "dotenv": "^8.2.0",
    "graphql-middleware": "4.0.1",
    "graphql-shield": "7.3.6",
    "graphql-yoga": "1.18.3",
    "jsonwebtoken": "8.5.1",
    "nexus-plugin-prisma": "^0.19.0",
    "querystring": "^0.2.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "2.4.2",
    "@types/jest": "^26.0.14",
    "@types/jsonwebtoken": "8.5.0",
    "@types/node": "12.12.58",
    "@types/ws": "7.2.6",
    "ts-node": "9.0.0",
    "ts-node-dev": "1.0.0-pre.50",
    "typescript": "4.0.2"
  },
  "prettier": {
    "singleQuote": true,
    "semi": false,
    "trailingComma": "all"
  },
  "engines": {
    "node": ">=10.0.0"
  }
}
